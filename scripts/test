#!/usr/bin/env bash

set -e

cd "$(dirname "$0")/.."

# Refuse to silently replace the Home Assistant test stack. The plugin is
# generated for a specific Home Assistant release and must stay in lockstep.
uv run --locked pytest tests/ -v
